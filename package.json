{
  "name": "gibrandom",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/contracts",
    "packages/contracts-hh",
    "packages/indexer",
    "packages/provider"
  ],
  "scripts": {
    "coverage": "npm run coverage --workspace=*",
    "build": "npm run build --workspace=*",
    "start": "npm run start --workspace=*",
    "dev": "npm run dev --workspace=*",
    "test": "npm run test --workspace=*"
  }
}
