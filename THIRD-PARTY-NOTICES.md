# Third-Party Notices

This repository as a whole is licensed under **AGPL-3.0-only** (see `LICENSE`).
It incorporates third-party components under their own licenses, listed below.
Those components retain their original licenses; the combined/distributed work is
governed by AGPL-3.0-only.

## GPL-3.0 components (copyleft — combination permitted by GPL-3.0 §13 / AGPL-3.0 §13)

GPL-3.0 and AGPL-3.0 are explicitly compatible: GPL-3.0 §13 and AGPL-3.0 §13
permit combining a GPL-3.0 work with an AGPL-3.0 work into a single combined work,
with the AGPL network-use requirement applying to the combination.

- **`packages/contracts/contracts/vendor/uzkge/`** — the Zypher `uzkge` zero-knowledge
  shuffle/reveal Solidity verifiers.
  - Upstream: https://github.com/zypher-game/uzkge — **GPL-3.0-only**.
  - Copied **verbatim** from the pinned upstream commit `2ae729dbc1b003733e47783a9a418a7b8a215fc5`
    (see `packages/contracts/contracts/vendor/VENDOR.md`). The pin is a consensus constant:
    the prover WASM and these verifiers must come from the same commit.
  - Upstream marks these Solidity files `SPDX-License-Identifier: UNLICENSED` while the
    Rust/WASM source is GPL-3.0-only — a known upstream contradiction. **We treat these
    files as GPL-3.0-only** (posture recorded in VENDOR.md). The files are left byte-for-byte
    as vendored to preserve the consensus pin; this notice governs their license.
  - `uzkge/libraries/BytesLib.sol` is the public-domain `solidity-bytes-utils` (Unlicense).

- **`@zypher-game/secret-engine`** (the shuffle/reveal prover WASM; a devDependency used only
  on the `feat/holdem-gate2-shuffle` branch) — built from the same GPL-3.0-only `uzkge`.
  The npm package self-tags MIT but ships no LICENSE file and has no public source repo;
  **we treat it as GPL-3.0-only**. It is used to generate proofs; it is not modified.

## Permissive components (Apache-2.0 / MIT / ISC / public domain — AGPL-compatible)

- **`packages/contracts/contracts/zk/generated/DiceSettleHonkVerifier.sol`** — generated by
  Aztec Barretenberg (`bb`). Copyright Aztec; Apache-2.0.
- **`@aztec/bb.js`** (MIT) and **`@noir-lang/*`** (MIT OR Apache-2.0) — the Noir/UltraHonk
  toolchain used by `examples/games/zk-settle` to compile circuits and generate the
  on-chain verifier above.
- **`examples/games/zk-settle/vendor/keccak256/`** — vendored keccak helper (see its headers).
- Arkworks (`ark-*`) transitive dependencies — Apache-2.0.

## Sub-packages that keep their own (permissive) license

These are intentionally reusable standalone utilities and remain permissively licensed
within this AGPL-3.0 repository (permissive licenses are AGPL-compatible):

- **`@gibs/pow-grinder`** (`examples/games/pow-grinder`) — MIT.
- **`@gibs/random-provider`** (`packages/provider`) — ISC.

## Notes / open items (from VENDOR.md)

Before any mainnet / real-money use of the ZK card stack:
- Regenerate the KZG SRS from a public ceremony and re-derive the uzkge `VerifierKey`
  constants with upstream `gen-params`.
- Obtain upstream (Zypher) license clarification for `uzkge` / `@zypher-game/secret-engine`.
- The vendored verifiers have not been independently audited.
